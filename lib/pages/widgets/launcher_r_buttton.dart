import 'package:flutter/material.dart';

class LauncherRButton extends StatelessWidget {
  const LauncherRButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
